{
  "swagger": "2.0",
  "paths": {},
  "definitions": {
    "Envelope": {
      "type": "object",
      "title": "Envelope is the canonical response body type.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "integer",
          "format": "int64",
          "x-go-name": "ID"
        },
        "name": {
          "type": "string",
          "x-go-name": "Name"
        }
      },
      "x-go-package": "github.com/go-openapi/codescan/fixtures/enhancements/alias-response"
    }
  },
  "responses": {
    "aliasedTopResponse": {
      "description": "AliasedTopResponse annotates an alias as the response: the scanner\nresolves it via responseBuilder.buildAlias under RefAliases=true.",
      "schema": {
        "$ref": "#/definitions/Envelope"
      }
    },
    "aliasedTopResponse2": {
      "description": "AliasedTopResponse2 chains AliasedTopResponse through a second alias.",
      "schema": {
        "$ref": "#/definitions/Envelope"
      }
    }
  }
}