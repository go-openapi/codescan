{
  "swagger": "2.0",
  "paths": {},
  "definitions": {
    "EnvelopeAliasModeled": {
      "description": "EnvelopeAliasModeled is the annotated counterpart of\nEnvelopeAlias. With `swagger:model` the alias opts in as a\nfirst-class spec entity; R8 then preserves the alias name at\nbody field sites in `swagger:response` structs.",
      "$ref": "#/definitions/ResponseEnvelope"
    },
    "Payload": {
      "type": "object",
      "title": "Payload is the canonical struct referenced by aliases.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "integer",
          "format": "int64",
          "x-go-name": "ID"
        },
        "name": {
          "type": "string",
          "x-go-name": "Name"
        }
      },
      "x-go-package": "github.com/go-openapi/codescan/fixtures/enhancements/alias-expand"
    },
    "PayloadAliasModeled": {
      "description": "Same underlying Go type, but `swagger:model` opts the alias in as a\nfirst-class spec entity.",
      "title": "PayloadAliasModeled is the annotated counterpart of `PayloadAlias`.",
      "$ref": "#/definitions/Payload"
    },
    "ResponseEnvelope": {
      "description": "ResponseEnvelope is the canonical struct referenced by aliases used in\nresponses. Its `payload` field is typed as the UNannotated `PayloadAlias`,\nso under R6 the field's $ref dissolves to `Payload`.",
      "type": "object",
      "properties": {
        "payload": {
          "$ref": "#/definitions/Payload"
        }
      },
      "x-go-package": "github.com/go-openapi/codescan/fixtures/enhancements/alias-expand"
    },
    "ResponseEnvelopeModeled": {
      "description": "ResponseEnvelopeModeled is the bidirectional sibling of\n`ResponseEnvelope`. Same struct shape, but its `payload` field uses\nthe annotated alias `PayloadAliasModeled`. Per R6, the field's $ref\npreserves the alias identity (`$ref: PayloadAliasModeled`),\ndemonstrating that the explicit `swagger:model` opt-in recovers the\npre-R6 alias-name $ref behaviour.",
      "type": "object",
      "properties": {
        "payload": {
          "$ref": "#/definitions/PayloadAliasModeled"
        }
      },
      "x-go-package": "github.com/go-openapi/codescan/fixtures/enhancements/alias-expand"
    }
  },
  "responses": {
    "aliasedModeledResponse": {
      "description": "AliasedModeledResponse is the bidirectional sibling of\nAliasedResponse: same shape, but its Body field uses the\nANNOTATED alias `EnvelopeAliasModeled`. Under R8 the body\nschema's `$ref` preserves `EnvelopeAliasModeled` (alongside\nthe alias's own definition), while AliasedResponse dissolves\nto `$ref: ResponseEnvelope`. Both halves on one canvas.",
      "schema": {
        "$ref": "#/definitions/EnvelopeAliasModeled"
      }
    },
    "aliasedResponse": {
      "description": "AliasedResponse has a body field whose type is an alias chain.",
      "schema": {
        "$ref": "#/definitions/ResponseEnvelope"
      }
    },
    "namedTopResponse": {
      "description": "NamedTopResponse is a plain struct annotated as a response — used to\nkeep a deterministic response in the expand-mode fixture even though\nresponse-level aliasing is deferred to the alias-response fixture.",
      "schema": {
        "$ref": "#/definitions/ResponseEnvelope"
      }
    }
  }
}